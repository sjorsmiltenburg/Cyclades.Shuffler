<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:Cyclades.Shuffler.ViewModels;assembly=Cyclades.Shuffler"
             x:Class="Cyclades.Shuffler.Views.StartPage">
  <StackLayout>
    <TableView>
      <TableRoot>
        <TableSection >
          <TextCell Text="Start a game" Detail="This app helps you play Cyclades the boardgame"></TextCell>
        </TableSection>
      </TableRoot>
    </TableView>
    <Label Text="Choose the number of players"></Label>
    <Button Text="2" Command="{Binding Start2PlayerGameCommand}"></Button>
    <Button Text="3" Command="{Binding Start3PlayerGameCommand}"></Button>
    <Button Text="4" Command="{Binding Start4PlayerGameCommand}"></Button>
    <Button Text="5" Command="{Binding Start5PlayerGameCommand}"></Button>
    <Label Text="{Binding MainText}" VerticalOptions="Center" HorizontalOptions="Center" />
  </StackLayout>

</ContentPage>